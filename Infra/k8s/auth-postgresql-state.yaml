apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db-statefulset1
  labels:
    app: postgresql-statefulset1
spec:
  serviceName: 'postgresql-service1'
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-statefulset1
  template:
    metadata:
      labels:
        app: postgresql-statefulset1
    spec:
      containers:
      - name: authdb-container
        image: postgres:14.12-alpine3.20
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: authapp-secret
              key: db-username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: authapp-secret
              key: db-password
        - name: POSTGRES_DB
          value: authDB
        volumeMounts:
        - name: db-persistent-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1"
  volumeClaimTemplates:
  - metadata:
      name: db-persistent-storage
    spec:
      accessModes: ['ReadWriteOnce']
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql-service1
spec:
  selector:
    app: postgresql-statefulset1
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
